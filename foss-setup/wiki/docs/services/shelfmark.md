# shelfmark

Shelfmark — book search/request/download frontend (replaces libreseerr+Bookshelf+rreading-glasses).

| | |
|---|---|
| **Host** | [nas](../hosts/nas.md) |
| **URL** | https://shelfmark.tabaska.us |
| **Source** | `foss-setup/configs/nas/shelfmark/docker-compose.yml` |

## Containers

| Service | Image (pinned) | Ports |
|---|---|---|
| `shelfmark` | `ghcr.io/calibrain/shelfmark:latest` | `8084:8084` |

## Volumes

| Service | Volume |
|---|---|
| `shelfmark` | `/volume1/docker/shelfmark/config:/config` |
| `shelfmark` | `/volume1/docker/calibre-web-automated/ingest:/books` |
| `shelfmark` | `/volume1/mounts/seedbox-files:/home/hd34/btabaska/files:rslave` |

## Operations

```bash
# NAS stack — manage via DSM Container Manager (project: shelfmark)
# or over SSH (sudo required): cd /volume1/docker/shelfmark && sudo docker compose ps
```

Update procedure: [Runbooks → Update images](../runbooks/update-images.md). Full add/change loop: [Runbooks → Add a service](../runbooks/add-a-service.md).

*Generated by `scripts/docs/gen-wiki-services.py` — do not edit by hand; edit the compose file and regenerate.*
