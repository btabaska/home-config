# dockge

Dockge — lightweight Docker Compose stack manager (SIMPLE DEFAULT)

| | |
|---|---|
| **Host** | [mini](../hosts/mini.md) |
| **URL** | https://dockge.tabaska.us |
| **Source** | `foss-setup/configs/docker-stack/stacks/dockge/compose.yaml` |
| **Upstream docs** | <https://github.com/louislam/dockge> |

## Containers

| Service | Image (pinned) | Ports |
|---|---|---|
| `dockge` | `louislam/dockge:1.5.0` | `5001:5001` |

## Volumes

| Service | Volume |
|---|---|
| `dockge` | `/var/run/docker.sock:/var/run/docker.sock` |
| `dockge` | `./data:/app/data` |
| `dockge` | `/opt/stacks:/opt/stacks` |

## Environment (`.env`)

Variable names from `.env.example` — real values live in `.env` on the host, sourced from the vault (never committed):

- `PUID`
- `PGID`

## Operations

```bash
ssh mini 'cd /opt/stacks/dockge && docker compose ps'
ssh mini 'cd /opt/stacks/dockge && docker compose logs --tail 50'
ssh mini 'cd /opt/stacks/dockge && docker compose pull && docker compose up -d'
```

Update procedure: [Runbooks → Update images](../runbooks/update-images.md). Full add/change loop: [Runbooks → Add a service](../runbooks/add-a-service.md).

*Generated by `scripts/docs/gen-wiki-services.py` — do not edit by hand; edit the compose file and regenerate.*
