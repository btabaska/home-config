# healthchecks

Healthchecks — dead-man's-switch monitoring for cron/backup jobs

| | |
|---|---|
| **Host** | [mini](../hosts/mini.md) |
| **URL** | https://healthchecks.tabaska.us |
| **Source** | `foss-setup/configs/docker-stack/stacks/healthchecks/compose.yaml` |
| **Upstream docs** | <https://healthchecks.io/docs/self_hosted_docker/> |

## Containers

| Service | Image (pinned) | Ports |
|---|---|---|
| `healthchecks` | `healthchecks/healthchecks:v3.10` | `8001:8000` |
| `db` | `postgres:17-alpine` | — |

## Volumes

| Service | Volume |
|---|---|
| `db` | `./db:/var/lib/postgresql/data` |

## Environment (`.env`)

Variable names from `.env.example` — real values live in `.env` on the host, sourced from the vault (never committed):

- `TZ`
- `HC_SECRET_KEY`
- `HC_SITE_ROOT`
- `HC_ALLOWED_HOSTS`
- `HC_DEFAULT_FROM_EMAIL`
- `HC_DB_PASSWORD`
- `HC_SUPERUSER_EMAIL`
- `HC_SUPERUSER_PASSWORD`

## Operations

```bash
ssh mini 'cd /opt/stacks/healthchecks && docker compose ps'
ssh mini 'cd /opt/stacks/healthchecks && docker compose logs --tail 50'
ssh mini 'cd /opt/stacks/healthchecks && docker compose pull && docker compose up -d'
```

Update procedure: [Runbooks → Update images](../runbooks/update-images.md). Full add/change loop: [Runbooks → Add a service](../runbooks/add-a-service.md).

*Generated by `scripts/docs/gen-wiki-services.py` — do not edit by hand; edit the compose file and regenerate.*
