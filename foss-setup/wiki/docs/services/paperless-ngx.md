# paperless-ngx

Paperless-ngx — document management / OCR archive (scan once, find forever)

| | |
|---|---|
| **Host** | [mini](../hosts/mini.md) |
| **URL** | https://paperless.tabaska.us |
| **Source** | `foss-setup/configs/docker-stack/stacks/paperless-ngx/compose.yaml` |
| **Upstream docs** | <https://docs.paperless-ngx.com/setup/#docker> · <https://hub.docker.com/_/postgres> |

## Containers

| Service | Image (pinned) | Ports |
|---|---|---|
| `webserver` | `ghcr.io/paperless-ngx/paperless-ngx:2.20.11` | `8000:8000` |
| `broker` | `postgres:17-alpine` | — |
| `redis` | `redis:7-alpine` | — |
| `gotenberg` | `gotenberg/gotenberg:8.21` | — |
| `tika` | `apache/tika:3.2.1.0-full` | — |

## Volumes

| Service | Volume |
|---|---|
| `webserver` | `./data:/usr/src/paperless/data` |
| `webserver` | `./media:/usr/src/paperless/media` |
| `webserver` | `./consume:/usr/src/paperless/consume` |
| `broker` | `./pgdata:/var/lib/postgresql/data` |
| `redis` | `./redis:/data` |

## Environment (`.env`)

Variable names from `.env.example` — real values live in `.env` on the host, sourced from the vault (never committed):

- `TZ`
- `PUID`
- `PGID`
- `PAPERLESS_SECRET_KEY`
- `PAPERLESS_DBPASS`
- `PAPERLESS_OCR_LANGUAGE`
- `PAPERLESS_URL`
- `PAPERLESS_ADMIN_USER`
- `PAPERLESS_ADMIN_PASSWORD`

## Operations

```bash
ssh mini 'cd /opt/stacks/paperless-ngx && docker compose ps'
ssh mini 'cd /opt/stacks/paperless-ngx && docker compose logs --tail 50'
ssh mini 'cd /opt/stacks/paperless-ngx && docker compose pull && docker compose up -d'
```

Update procedure: [Runbooks → Update images](../runbooks/update-images.md). Full add/change loop: [Runbooks → Add a service](../runbooks/add-a-service.md).

*Generated by `scripts/docs/gen-wiki-services.py` — do not edit by hand; edit the compose file and regenerate.*
