# romm

RomM — self-hosted ROM manager + web library (retro-02 / game-14)

| | |
|---|---|
| **Host** | [mini](../hosts/mini.md) |
| **URL** | https://romm.tabaska.us |
| **Source** | `foss-setup/configs/docker-stack/stacks/romm/compose.yaml` |
| **Notes** | RomM retro-game library (container port 8080; mariadb sidecar). |
| **Upstream docs** | <https://docs.romm.app> |

## Containers

| Service | Image (pinned) | Ports |
|---|---|---|
| `romm` | `rommapp/romm:latest` | `8998:8080` |
| `romm-db` | `mariadb:11` | — |

## Volumes

| Service | Volume |
|---|---|
| `romm` | `romm_resources:/romm/resources` |
| `romm` | `romm_redis_data:/redis-data` |
| `romm` | `/mnt/share/Games/romm:/romm/library` |
| `romm` | `./assets:/romm/assets` |
| `romm` | `./config:/romm/config` |
| `romm-db` | `romm_db_data:/var/lib/mysql` |

## Environment (`.env`)

Variable names from `.env.example` — real values live in `.env` on the host, sourced from the vault (never committed):

- `TZ`
- `ROMM_DB_ROOT_PASSWD`
- `ROMM_DB_PASSWD`
- `ROMM_AUTH_SECRET_KEY`
- `IGDB_CLIENT_ID`
- `IGDB_CLIENT_SECRET`

## Operations

```bash
ssh mini 'cd /opt/stacks/romm && docker compose ps'
ssh mini 'cd /opt/stacks/romm && docker compose logs --tail 50'
ssh mini 'cd /opt/stacks/romm && docker compose pull && docker compose up -d'
```

Update procedure: [Runbooks → Update images](../runbooks/update-images.md). Full add/change loop: [Runbooks → Add a service](../runbooks/add-a-service.md).

*Generated by `scripts/docs/gen-wiki-services.py` — do not edit by hand; edit the compose file and regenerate.*
