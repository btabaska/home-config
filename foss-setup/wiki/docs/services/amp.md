# amp

AMP (CubeCoders) game panel — containerized so it never touches the CachyOS host (ImageGenius image).

| | |
|---|---|
| **Host** | [rig](../hosts/rig.md) |
| **URL** | https://amp.tabaska.us |
| **Source** | `foss-setup/configs/gaming/amp/compose.yaml` |
| **Notes** | CubeCoders AMP game panel (MinecraftCross Java+Bedrock instance). Licence is hostname-pinned — never change the compose hostname casually. |

## Containers

| Service | Image (pinned) | Ports |
|---|---|---|
| `amp` | `ghcr.io/imagegenius/amp:latest` | `8080:8080`, `8000-8010:8000-8010/udp`, `25565:25565`, `19132:19132/udp` |

## Volumes

| Service | Volume |
|---|---|
| `amp` | `./config:/config` |

## Operations

```bash
ssh rig 'cd /opt/stacks/amp && sudo docker compose ps'
ssh rig 'cd /opt/stacks/amp && sudo docker compose logs --tail 50'
ssh rig 'cd /opt/stacks/amp && sudo docker compose pull && sudo docker compose up -d'
```

Update procedure: [Runbooks → Update images](../runbooks/update-images.md). Full add/change loop: [Runbooks → Add a service](../runbooks/add-a-service.md).

*Generated by `scripts/docs/gen-wiki-services.py` — do not edit by hand; edit the compose file and regenerate.*
