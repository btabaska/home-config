# unbound

Unbound — recursive, validating, DNSSEC-aware resolver (the upstream for AdGuard)

| | |
|---|---|
| **Host** | [mini](../hosts/mini.md) |
| **URL** | — (no web UI / not proxied) |
| **Source** | `foss-setup/configs/docker-stack/stacks/unbound/compose.yaml` |
| **Upstream docs** | <https://docs.pi-hole.net/guides/dns/unbound/> |

## Containers

| Service | Image (pinned) | Ports |
|---|---|---|
| `unbound` | `mvance/unbound:1.22.0` | `127.0.0.1:5335:5335/tcp`, `127.0.0.1:5335:5335/udp` |

## Volumes

| Service | Volume |
|---|---|
| `unbound` | `./unbound:/opt/unbound/etc/unbound` |

## Operations

```bash
ssh mini 'cd /opt/stacks/unbound && docker compose ps'
ssh mini 'cd /opt/stacks/unbound && docker compose logs --tail 50'
ssh mini 'cd /opt/stacks/unbound && docker compose pull && docker compose up -d'
```

Update procedure: [Runbooks → Update images](../runbooks/update-images.md). Full add/change loop: [Runbooks → Add a service](../runbooks/add-a-service.md).

*Generated by `scripts/docs/gen-wiki-services.py` — do not edit by hand; edit the compose file and regenerate.*
