# wallabag

Wallabag — self-hosted read-it-later (replaces Pocket)

| | |
|---|---|
| **Host** | [mini](../hosts/mini.md) |
| **URL** | https://wallabag.tabaska.us |
| **Source** | `foss-setup/configs/docker-stack/wallabag/docker-compose.yml` |
| **Upstream docs** | <https://github.com/wallabag/docker> · <https://doc.wallabag.org/admin/installation/installation/> · <https://doc.wallabag.org/admin/parameters/> |

## Containers

| Service | Image (pinned) | Ports |
|---|---|---|
| `wallabag` | `wallabag/wallabag:2.6.14` | `8085:80` |
| `db` | `mariadb:11.4` | — |
| `redis` | `redis:7-alpine` | — |

## Volumes

| Service | Volume |
|---|---|
| `wallabag` | `./data:/var/www/wallabag/data` |
| `wallabag` | `./images:/var/www/wallabag/web/assets/images` |
| `db` | `./db:/var/lib/mysql` |
| `redis` | `./redis:/data` |

## Environment (`.env`)

Variable names from `.env.example` — real values live in `.env` on the host, sourced from the vault (never committed):

- `WALLABAG_DB_ROOT_PASSWORD`
- `WALLABAG_DB_PASSWORD`
- `WALLABAG_SECRET`
- `WALLABAG_POPULATE_DATABASE`
- `WALLABAG_DOMAIN_NAME`
- `WALLABAG_SERVER_NAME`
- `WALLABAG_MAILER_DSN`
- `WALLABAG_FROM_EMAIL`

## Operations

```bash
ssh mini 'cd /opt/stacks/wallabag && docker compose ps'
ssh mini 'cd /opt/stacks/wallabag && docker compose logs --tail 50'
ssh mini 'cd /opt/stacks/wallabag && docker compose pull && docker compose up -d'
```

Update procedure: [Runbooks → Update images](../runbooks/update-images.md). Full add/change loop: [Runbooks → Add a service](../runbooks/add-a-service.md).

*Generated by `scripts/docs/gen-wiki-services.py` — do not edit by hand; edit the compose file and regenerate.*
