# mealie

Mealie — self-hosted recipe manager + meal planner + shopping list

| | |
|---|---|
| **Host** | [mini](../hosts/mini.md) |
| **URL** | https://recipes.tabaska.us |
| **Source** | `foss-setup/configs/docker-stack/stacks/mealie/compose.yaml` |
| **Upstream docs** | <https://docs.mealie.io/> |

## Containers

| Service | Image (pinned) | Ports |
|---|---|---|
| `mealie` | `ghcr.io/mealie-recipes/mealie:v3.4.0` | `9000:9000` |

## Volumes

| Service | Volume |
|---|---|
| `mealie` | `./data:/app/data` |

## Environment (`.env`)

Variable names from `.env.example` — real values live in `.env` on the host, sourced from the vault (never committed):

- `TZ`
- `PUID`
- `PGID`
- `MEALIE_BASE_URL`
- `MEALIE_SECRET`

## Operations

```bash
ssh mini 'cd /opt/stacks/mealie && docker compose ps'
ssh mini 'cd /opt/stacks/mealie && docker compose logs --tail 50'
ssh mini 'cd /opt/stacks/mealie && docker compose pull && docker compose up -d'
```

Update procedure: [Runbooks → Update images](../runbooks/update-images.md). Full add/change loop: [Runbooks → Add a service](../runbooks/add-a-service.md).

*Generated by `scripts/docs/gen-wiki-services.py` — do not edit by hand; edit the compose file and regenerate.*
