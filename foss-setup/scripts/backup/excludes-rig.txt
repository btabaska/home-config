# restic excludes for rig (CachyOS gaming box) — deployed to /etc/restic/excludes.txt
# (referenced by RESTIC_EXCLUDE_FILE in /etc/restic/env). One pattern per line.
# Backup set (see BACKUP_PATHS in /etc/restic/env): /etc + /home/btabaska + the
# game/AI *state* under /opt/stacks (Palworld + AMP worlds) + the litellm-db and
# open-webui docker volumes (fix-20, 2026-07-16 — a disk loss must not lose game/AI
# data). Keep it LEAN Tier-1: worlds, saves and configs YES; re-downloadable game
# binaries, server jars, LLM model weights and media NO.

# Games / game platforms / wine prefixes
/home/btabaska/Games
/home/btabaska/.local/share/Steam
/home/btabaska/.steam
/home/btabaska/.local/share/lutris
/home/btabaska/.local/share/bottles
/home/btabaska/.local/share/wineprefixes
/home/btabaska/.wine
/home/btabaska/Desktop/Portable_LAN_Games_Repo

# LLM runtimes + model weights (re-downloadable, hundreds of GB)
/home/btabaska/.ollama
/home/btabaska/.lmstudio
/home/btabaska/Documents/textgen-ik-4.6.2
/home/btabaska/Documents/textgen-portable-ik-4.6.2-linux-cuda13.1.tar.gz
/home/btabaska/Desktop/text-generation-webui-3.17
/home/btabaska/Desktop/DavidAUgemma-4-31B-it-The-DECKARD-HERETIC-UNCENSORED-Thinking

# Bulk media / transient (Music lives with the media tier, not restic Tier-1)
/home/btabaska/Music
/home/btabaska/Downloads
/home/btabaska/Applications

# Package-manager / toolchain caches (regenerable)
/home/btabaska/.cache
/home/btabaska/.npm
/home/btabaska/.bun
/home/btabaska/.cargo
/home/btabaska/.rustup
/home/btabaska/.local/share/pipx

# ansible-pull working checkout — a git clone of the repo, regenerable
/home/btabaska/.ansible-pull

# Per-project deps (also caught by the wrapper's **/node_modules)
/home/btabaska/Projects/*/node_modules

# Electron/browser disk caches anywhere
**/Cache
**/GPUCache
**/Code Cache
**/CachedData

# --- game-server data IS in the backup set now (fix-20): keep worlds + server
# configs, drop the re-downloadable AMP/Minecraft binaries so B2 stays lean.
# (MinecraftCross01's instance dir is ~13G, almost all jars/libraries.) ---
/opt/stacks/amp/config/.ampdata/instances/*/Minecraft/libraries
/opt/stacks/amp/config/.ampdata/instances/*/Minecraft/versions
/opt/stacks/amp/config/.ampdata/instances/*/Minecraft/cache
/opt/stacks/amp/config/.ampdata/instances/*/Minecraft/crash-reports
/opt/stacks/amp/config/.ampdata/instances/*/Minecraft/logs
# AMP's own local backup zips (fix-34): compressed hourly zips of the same world
# restic already snapshots — they dedup terribly and bloated B2 by ~12G. The live
# world dir stays in the set; AMP zips are a local-only convenience tier.
/opt/stacks/amp/config/.ampdata/instances/*/Backups
# server + mod/plugin JARs are re-downloadable from the modpack/version manifest
**/*.jar
