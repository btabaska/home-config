# Handoff queue

Ordered work queue for the quality/reliability workstream. The `00-MASTER` prompt
drives this: a fresh session does the **next unchecked item whose deps are all
checked and that is marked `autonomous`**, checks it off, commits, and stops.
Items marked `needs-user` or `collaborative` PAUSE the loop (report to the user).

Status: `[ ]` pending · `[x]` done · `[~]` in progress / partial (see note).

- [~] **01** — Acceptance-test framework + movie/TV test + done-audit (#5/#6/#10) · **autonomous** · deps: none · `01-acceptance-test-framework.md`
  - **#5 design doc DONE** (`wiki/docs/runbooks/acceptance-testing.md` — framework + journey catalog + conventions, in nav). **#6 movie/TV check + #10 done-audit BLOCKED:** Plex on the NAS is returning **HTTP 503 to all clients** (metadata-analysis storm on §4/YouTube, a side effect of today's 1363-item Pinchflat ingest) — can't live-validate/negative-test a Plex journey while Plex 503s. User decision 2026-07-13: **let Plex drain on its own** (no restart). Resume #6 + #10 once Plex serves 200 again (re-check `pinchflat-plex-visible`; see memory [[plex-bulk-ingest-analysis-storm]]).
- [x] **02** — HA reliability: backups + Assist endpoint (#11/#12) · **autonomous** · deps: none · `02-ha-reliability.md` — DONE 2026-07-13: off-eMMC encrypted NAS backups (agent `hassio.nas_backups`, daily 04:45, retention 3, validated landing on NAS) + Assist wired to rig Ollama (`conversation.rig_ollama_assist`, live completion). 2 negative-tested checks. **User action:** confirm the backup encryption key (vault `hosts.ha.backup_password`) is in Bitwarden.
- [~] **03** — DR reproducibility: ansible SOPS gate + NAS Tier-1→B2 verify (#13/#14) · **autonomous** (saving the age key may need the user) · deps: none · `03-dr-reproducibility.md`
  - **#14 DONE 2026-07-13:** NAS Tier-1 Hyper Backup → B2 **verified live and healthy** — task "S3 Backup 1" → `bucket-hyper-backup`/`TabaskaNAS_1.hbk`, covers `/backups /docker /docs /homes /photo` (= all shares with real data; `vault`/`appdata` are empty 28K shells superseded by `/docker`), last success 2026-07-12 19:28. **nas-02 closed** (was stale "pending"). New negative-tested dead-man `nas-hyperbackup-b2-fresh` (crit, 50h). ⚠️ **client-side encryption is OFF** (`enable_data_encrypt=false`) — flagged for a user decision (fixing = destructive task re-create).
  - **#13 BLOCKED [~]** — live probing (2026-07-13) found the gap is bigger than "seed the secret", and closing it safely needs the user + is too risky for one autonomous pass on the single off-site DR path: **(a)** the ansible `backup` role **diverged** from the curated live setup — live runs `scripts/backup/restic-backup.sh` sourcing `/etc/restic/env` with a Healthchecks ping + ntfy + pre-backup DB dumps, but the role writes a *different* unit (`/etc/restic/backup.env`, inline `restic backup`, none of the extras). Seeding the SOPS secret as-is would make the next `ansible-pull` **clobber** the tuned live DR units. Correct fix = rewrite the role to reproduce the live artifacts (per-host `host_vars` + per-host SOPS env). **(b)** `sops` **and** `age` binaries are **missing on both hosts** → the `community.sops` lookup can't decrypt even with the key+secret present; a reproducible rebuild must also install them (age is in apt/pacman; sops needs a release `.deb` on Ubuntu). **(c)** the **rig has no ansible at all** (its `ansible-pull.timer` is enabled-but-broken) → can't validate the pull path there; separate bootstrap gap. **(d)** the age key can be generated+wired autonomously but **saving it to Bitwarden/paper is a user step**. **Next session** (ideally with the user for the age-key save + the sops `.deb` install ack): rewrite the role → install sops/age → generate+place age key → seed per-host SOPS env with the *exact live* `/etc/restic/env` values (so the same B2 repo/password → snapshots restorable) → `ansible-pull --tags backup --check --diff` on mini until it's a **no-op** vs live → then one real converge to prove idempotent re-arm. **Do not** seed the secret before the role is aligned. Restic dead-man for mini+rig already exists and is FRESH (harden step pre-satisfied).
- [x] **04** — Network re-audit vs plan (#15) · **autonomous** (may need UniFi UI confirmation) · deps: none · `04-network-reaudit.md` — DONE 2026-07-13: **operator confirmed subnets + zone rules are implemented as directed and declined router access** for a machine audit. Fleet-corroborated (no router): all 6 VLAN gateways live (`.1` on subnets 1/10/20/30/40/50), Trusted=192.168.10.0/24, IoT=192.168.20.0/24 (Hue `.20.100`), Trusted→IoT allow works. Docs corrected (`vlan-zone-firewall-plan.md` — subnets marked confirmed-real + #16/#4 gap list appended); memory `mini-dhcp-lease-outage` corrected (static IP **was** applied 2026-07-10, outage class closed). New negative-tested invariant check `net-trusted-to-iot-reachable` (warn). Router-internal specifics (exact ZBF policy list, per-VLAN mDNS proxy, IGMP snooping) are operator-confirmed, not machine-verified → enumerated as #16/#4 follow-ups.
- [ ] **05** — DNS-chain tail dns-03/04/05 (#16) · **needs-user** (manual UniFi DHCP/UI) · deps: 04 · `05-dns-tail.md`
- [ ] **06** — HomePod ↔ HA HomeKit hub (#4) · **needs-user** (on-device Apple Home) · deps: 04 · `06-homepod-ha.md`
- [ ] **07** — Walk & prune the unbuilt roadmap (#17) · **collaborative** (user decisions) · deps: none · `07-roadmap-prune.md`

Not queued (do not auto-start):
- **#18** credential rotations — **deferred** until the user says the build phase is done. (Also: `sudo.nas_password` sits in git history — rotate it here.)
- **#19** NAS parity — **backlog**, no new spend now.

_Completion log (append one line per finished item):_
- 2026-07-13 · **01 partial [~]** · #5 acceptance-test framework design doc shipped (`wiki/docs/runbooks/acceptance-testing.md` + nav) — framework, two check shapes, conventions, journey catalog w/ ✅/gaps. #6/#10 blocked on live Plex 503 (analysis storm; user chose let-it-drain). Commit `fd197ae` / fj `839fd06`.
- 2026-07-13 · **04 done [x]** · Network re-audit (#15): operator confirmed subnets+zone rules implemented as directed + declined router access. Fleet corroboration (no router): 6 VLAN gateways live, Trusted=10.0/24, IoT=20.0/24 (Hue), Trusted→IoT allow works, dns-03 chain applied on mini (now static). Corrected `vlan-zone-firewall-plan.md` (subnets confirmed-real + #16/#4 gap list) + memory `mini-dhcp-lease-outage` (static IP applied 2026-07-10). New negative-tested check `net-trusted-to-iot-reachable` (warn, `network.yaml`). Commit `<hash>` / fj `<hash>`.
- 2026-07-13 · **03 partial [~]** · #14 NAS Tier-1 Hyper Backup → B2 **verified live + nas-02 closed** (task "S3 Backup 1", covers /backups /docker /docs /homes /photo = all real-data shares, last success 07-12 19:28; client-side encryption OFF — flagged). New negative-tested dead-man `nas-hyperbackup-b2-fresh` (crit, 50h) deployed + picked up by the daily sweep. #13 ansible SOPS gate **left [~]/blocked**: role diverged from live + sops/age binaries missing on hosts + rig has no ansible + age-key save needs user → unsafe to converge the single DR path this pass (precise plan in the item). Commit `7184614` / fj `1ab0f5c`.
- 2026-07-13 · **02 done [x]** · HA #11 off-eMMC encrypted NAS backups (SMB user `ha-backup` + CIFS mount `nas_backups`/agent `hassio.nas_backups`, daily 04:45, retention 3; validated encrypted tar landed on NAS + listable) & #12 Assist → rig Ollama (`conversation.rig_ollama_assist`, llama3.2:3b, live completion). Checks `ha-backup-offsite-fresh` (crit) + `ha-assist-rig-llm-reachable` (warn), both negative-tested. Stdlib WS client `scripts/ha/haws.py`. Commit `a1f935d` / fj `a1967f1`.
