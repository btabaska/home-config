# Infra service sweep (agent complete)
TOP 5: (1) HIGH stray mini Immich RESURRECTED by tonight's docker daemon restart (restart=always beats compose stop) — crash-looping again, unbacked-up, load driver; rec: compose down + remove from autostart (data kept). (2) HIGH Dependency-Track empty (0 projects) — mini sbom-nightly OOM-killed (syft dir:/ walks /var/lib/docker/overlay2; /etc/inventory/sbom.env lacks SCAN_EXCLUDES — same fix as NAS). (3) HIGH opportunity: CWA Kobo sync endpoint present but OFF (config_kobo_sync=0) — operator owns a Kobo; enable + token + books.tabaska.us/kobo/<token>. (4) MED Vaultwarden /admin exposed on public vhost + raw (non-argon2) ADMIN_TOKEN — add LAN/tailnet matcher + hash. (5) MED Caddy has ZERO security headers/HSTS/access logs fleet-wide — one reusable snippet fixes all vhosts.
ALSO: Forgejo admin has NO 2FA (control-repo forge!) + no push-mirror offsite (rec: mirror to private GitHub, cheap redundancy); Paperless docs stored on mini not NAS as planned (decide before archive grows) + no document_exporter scheduled + no IMAP ingest; D6 EVIDENCE: qbittorrent-nox 0 torrents in BT_backup, 11d idle — KILL rec; Deluge = real workhorse (~250 torrents, live fastresume). Immich NAS: QSV + HDD-mode correctly configured; verify mobile auto-backup on phone. Wallabag/Paperless/Miniflux keep default admin usernames (report only, passwords strong). VW push relay unconfigured (optional). Beszel/Dockge/etckeeper/chezmoi healthy. HA 200/401 as expected (run-5). Caddy has no dead active vhosts.
