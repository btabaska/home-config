# Media + YouTube wiring audit (agent complete)
HIGH: No Plex YouTube library (only Movies/TV/Music). Rec option A: "Other Videos" lib over /volume1/youtube, agent=Personal Media, scanner=Plex Video Files; Pinchflat embedded metadata/NFO carries titles. Option B: TV-type + YouTube-Agent.bundle (rich but legacy-plugin upkeep).
MEDIUM: Pinchflat is a BLANK install — 0 sources, 0 media profiles; nothing will ever download. Rec profile template /downloads/{{source}}/{{date}} - {{title}}.mp4, embed meta+thumb+nfo.
MEDIUM: MeTube audio not routed — set AUDIO_DOWNLOAD_DIR=/audio + NEW rw CIFS mount of //nas/music → /volume1/music/YouTube (current /mnt/nas/music on mini is ro; second rw mount w/ credentials file needed). Fan-out: Plex Music scan-on-change, Navidrome 1h rescan, iPod via existing manual Rhythmbox runbook.
MEDIUM: beets defined in repo compose (profile music-tags) but never deployed — option 2 for clean tags: MeTube → /volume1/staging/music-ingest → beet import cron → /volume1/music. Caveat: YouTube singles often miss MusicBrainz; quarantine no-match; beets must not reorganize Lidarr's tree.
HIGH: Kometa config = untouched template (plex url 192.168.1.12 WRONG subnet, no token, no TMDb key, refs nonexistent Anime lib + missing Music.yml) — daily 05:00 runs failing. Fill from vault + trim libs (media-02 open).
LOW: Maintainerr zero rules (empty shell, harmless). LOW: mini fstab music/games mounts embed plaintext password (youtube mount does it right — migrate the other two). LOW: Plex a few versions behind + transcoder core dump artifact; update at leisure.
OK: Plex HW transcode licensed+enabled (QSV), scan-on-change on, remote mapped; Tautulli collecting; Navidrome healthy (79 files, 1h scan, defaults fine); Stash sane; iPod = manual Rhythmbox sync from NAS master (documented).
