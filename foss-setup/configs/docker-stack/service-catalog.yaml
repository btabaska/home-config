# service-catalog.yaml — source of truth for the home surface (home-01..04).
# Every LIVE service in the homelab: what it is, where it runs, and its public
# https://<name>.tabaska.us front door (Caddy on the mini, 192.168.10.2).
#
# Verified live 2026-07-07 (docker ps on mini + HTTP probes from mini).
# Re-verified + backfilled 2026-07-09 (docs-debt audit): gaming stack (rig),
# romm/bedrock-connect/bgutil-pot (mini), NAS workers, recyclarr cron.
# hosts: mini=192.168.10.2, nas=192.168.10.4, rig=192.168.10.12 (24/7),
#        ha=192.168.10.50, seedbox=100.119.134.94 (Tailscale, "Betty")
#
# ui: false  => backend/agent with no web UI: no Caddy vhost, no Homepage tile.
# on_demand  => legacy field, kept for schema stability; all hosts run 24/7 now
#               (rig sleep retired 2026-07-08 — WoL is recovery tooling only).
#               A 502 through Caddy is an INCIDENT, not expected behavior.

services:
  # --- Media ----------------------------------------------------------------
  - name: plex
    category: Media
    host: nas
    port: 32400
    url: https://plex.tabaska.us
    on_demand: false
    notes: Movies & TV. Widget uses Plex token.
  - name: jellyfin
    category: Media
    host: nas
    port: 8096
    url: https://jellyfin.tabaska.us
    on_demand: false
    notes: FOSS fully-local media server (parallel to Plex, no plex.tv dependency). Widget uses API key.
  - name: metube
    category: Media
    host: mini
    port: 8081
    url: https://metube.tabaska.us
    on_demand: false
    notes: One-off YouTube downloads — on the edge Docker network (fix-102 corrected — was mislabelled host-network).
  - name: pinchflat
    category: Media
    host: mini
    port: 8945
    url: https://pinchflat.tabaska.us
    on_demand: false
    notes: YouTube channel archiving ("Sonarr for YouTube").
  - name: kometa
    category: Media
    host: mini
    port: null
    url: null
    on_demand: false
    ui: false
    notes: Plex collections/metadata batch job — no web UI, runs on schedule.
  - name: bgutil-pot
    category: Media
    host: mini
    port: 4416
    url: null
    on_demand: false
    ui: false
    notes: YouTube PO-token provider for yt-dlp — MeTube/Pinchflat point at http://bgutil-pot:4416 to beat the 403 JS/GVS challenge. Internal, no UI, no Caddy vhost.

  # --- Requests ---------------------------------------------------------------
  - name: seerr
    category: Requests
    host: mini
    port: 5055
    url: https://seerr.tabaska.us
    on_demand: false
    notes: Movie/TV request portal (Jellyseerr/Overseerr-compatible API).
  - name: musicseerr
    category: Requests
    host: mini
    port: 8688
    url: https://musicseerr.tabaska.us
    on_demand: false
    notes: Album request portal (feeds Lidarr).
  - name: libreseerr
    category: Requests
    host: mini
    port: 8789
    url: https://libreseerr.tabaska.us
    on_demand: false
    notes: Book request portal (feeds Bookshelf, the Readarr fork). Container port 5000. Being superseded by shelfmark.
  - name: shelfmark
    category: Requests
    host: nas
    port: 8084
    url: https://shelfmark.tabaska.us
    on_demand: false
    notes: Book search/request/download frontend (calibrain/shelfmark) — the successor to libreseerr+bookshelf+rreading-glasses. Downloads land directly in the CWA ingest folder. LAN/Tailscale only.

  # --- Media Automation -------------------------------------------------------
  - name: sonarr
    category: Media Automation
    host: nas
    port: 8989
    url: https://sonarr.tabaska.us
    on_demand: false
    notes: TV automation.
  - name: radarr
    category: Media Automation
    host: nas
    port: 7878
    url: https://radarr.tabaska.us
    on_demand: false
    notes: Movie automation.
  - name: lidarr
    category: Media Automation
    host: nas
    port: 8686
    url: https://lidarr.tabaska.us
    on_demand: false
    notes: Music automation.
  - name: readarr
    category: Media Automation
    host: nas
    port: null
    url: null
    on_demand: false
    notes: "DECOMMISSIONED 2026-07-20 (bmig-05) — replaced by bookshelf (the maintained Readarr fork) on NAS :8790; Caddy vhost removed, readarr.tabaska.us returns 000. Row kept for history."
  - name: bookshelf
    category: Media Automation
    host: nas
    port: 8790
    url: https://bookshelf.tabaska.us
    on_demand: false
    notes: Book automation — the maintained Readarr fork (pennydreadful/bookshelf), replaces readarr. Metadata via rreading-glasses; downloads feed the CWA ingest folder. Container port 8787.
  - name: prowlarr
    category: Media Automation
    host: nas
    port: 9696
    url: https://prowlarr.tabaska.us
    on_demand: false
    notes: Indexer manager for the *arr stack.
  - name: bazarr
    category: Media Automation
    host: nas
    port: 6767
    url: https://bazarr.tabaska.us
    on_demand: false
    notes: Subtitle automation for Sonarr + Radarr libraries.
  - name: bitmagnet
    category: Media Automation
    host: nas
    port: 3333
    url: https://bitmagnet.tabaska.us
    on_demand: false
    notes: Self-hosted DHT crawler + local Torznab indexer for Prowlarr (rate-limit-proof hedge).
  - name: flaresolverr
    category: Media Automation
    host: nas
    port: 8191
    url: https://flaresolverr.tabaska.us
    on_demand: false
    notes: Cloudflare-challenge solver for Prowlarr. API only — root returns a status JSON.
  - name: tautulli
    category: Media Automation
    host: mini
    port: 8181
    url: https://tautulli.tabaska.us
    on_demand: false
    notes: Plex analytics/history.
  - name: maintainerr
    category: Media Automation
    host: mini
    port: 6246
    url: null
    on_demand: false
    status: removed
    notes: REMOVED FROM PLAN 2026-07-08 — no auto-deletion wanted. (Caddy vhost lingered until 2026-07-09; now removed.) No compose retained (git history has it); stack dir removed from mini 2026-07-19, data archived to /opt/stacks/backups/maintainerr-data-2026-07-19.tar.gz (fix-43).
  - name: recyclarr
    category: Media Automation
    host: mini
    port: null
    url: null
    ui: false
    on_demand: false
    notes: TRaSH-guides sync — weekly cron (`docker compose run`), not a long-running container; dead-manned via healthchecks `recyclarr-sync-mini`.
  - name: beets
    category: Media Automation
    host: nas
    port: 8337
    url: null
    ui: true
    on_demand: false
    notes: Music tagger/importer (YouTube-audio path) with a web UI on :8337; watch for hung one-off `-run-` containers. (fix-102 corrected — was mislabelled no-UI/port-null).
  - name: soularr
    category: Media Automation
    host: nas
    port: null
    url: null
    ui: false
    on_demand: false
    notes: Lidarr↔slskd bridge worker — no UI.
  - name: unpackerr
    category: Media Automation
    host: nas
    port: 5656
    url: null
    ui: false
    on_demand: false
    notes: Archive extraction for *arr imports — no UI.
  - name: rreading-glasses
    category: Media Automation
    host: nas
    port: null
    url: null
    ui: false
    on_demand: false
    notes: Readarr metadata proxy (own postgres sidecar) — no UI.
  - name: deluge
    category: Media Automation
    host: seedbox
    port: 5945  # 5945 is our web port; 8112 is another seedbox tenant (fix-102)
    url: https://deluge.tabaska.us
    on_demand: false
    notes: Off-site torrents (Betty seedbox over Tailscale).
  - name: slskd
    category: Media Automation
    host: seedbox
    port: 5030
    url: https://slskd.tabaska.us
    on_demand: false
    notes: Soulseek daemon (Betty seedbox over Tailscale).

  # --- Photos -----------------------------------------------------------------
  - name: immich
    category: Photos
    host: nas
    port: 2283
    url: https://immich.tabaska.us
    on_demand: false
    notes: Photo library + mobile backup. Vhost verified 2026-07-07.

  # --- Music ------------------------------------------------------------------
  - name: navidrome
    category: Music
    host: mini
    port: 4533
    url: https://music.tabaska.us
    on_demand: false
    notes: Subsonic-compatible music streaming.

  # --- Reading & Docs -----------------------------------------------------------
  - name: calibre-web-automated
    category: Reading & Docs
    host: nas
    port: 8083
    url: https://books.tabaska.us
    on_demand: false
    notes: Ebook library (CWA).
  - name: audiobookshelf
    category: Reading & Docs
    host: nas
    port: 13378
    url: https://abs.tabaska.us
    on_demand: false
    notes: Audiobooks + podcasts server (read-16). Non-root; internal PORT=13378. Widget uses an API key.
  - name: komga
    category: Reading & Docs
    host: nas
    port: 25600
    url: https://komga.tabaska.us
    on_demand: false
    notes: Comics + manga reader (read-17). JVM app; non-root 1026:100; Comics library under /volume1/comics, Manga library under /volume1/manga (its own DSM share so read-18 Suwayomi can write to it over SMB). Widget uses username/password.
  - name: suwayomi
    category: Reading & Docs
    host: rig
    port: 4567
    url: https://manga.tabaska.us
    on_demand: false
    notes: Manga acquisition server (read-18). Tachidesk; runs Tachiyomi/Mihon extensions server-side and writes per-chapter CBZ to the NAS "manga" DSM share (/mnt/nas-manga -> /volume1/manga) which Komga's Manga library scans. No app auth (LAN posture like ollama/comfyui).
  - name: mylar3
    category: Reading & Docs
    host: nas
    port: 8090
    url: https://mylar.tabaska.us
    on_demand: false
    notes: Western-comics acquisition *arr (read-21..25) — Sonarr's role for comics. LSIO container on the NAS; PUID/PGID 1026:100 (matches Komga so grabbed CBZ stay readable). Primary acquisition = built-in GetComics DDL over plain HTTPS (read-22, no indexer/download client); torrent safety-net via Prowlarr app-sync + off-site Betty Deluge (read-23, best-effort). ComicVine API key = metadata authority (identity/catalog only, like TheTVDB to Sonarr). Output lands in /volume1/comics/Comics/<Series>/ which Komga's Comics library scans. Completes the comics/manga row — Mylar3 = western comics, Suwayomi = manga, Komga = shared reader. Homepage widget uses the Mylar3 API key.
  - name: paperless
    category: Reading & Docs
    host: mini
    port: 8000
    url: https://paperless.tabaska.us
    on_demand: false
    notes: Document archive + OCR (redis/db/gotenberg/tika sidecars, no UI of their own).
  - name: wallabag
    category: Reading & Docs
    host: mini
    port: 8085
    url: https://wallabag.tabaska.us
    on_demand: false
    notes: Read-it-later. Container port 80.
  - name: miniflux
    category: Reading & Docs
    host: mini
    port: 8082
    url: https://rss.tabaska.us
    on_demand: false
    notes: RSS reader. Container port 8080.
  - name: booklogr
    category: Reading & Docs
    host: mini
    port: 5000
    url: https://booklogr.tabaska.us
    on_demand: false
    notes: Personal reading tracker / library. Two containers & vhosts — React SPA at booklogr.tabaska.us (web, container :80) + Flask API at booklogr-api.tabaska.us (api, container :5000). Edge-only (no host port); SQLite.

  # --- Life ---------------------------------------------------------------------
  - name: mealie
    category: Life
    host: mini
    port: 9000
    url: https://recipes.tabaska.us
    on_demand: false
    notes: Recipes & meal planning. (Healthcheck flap seen 2026-07-07 resolved — healthy as of 2026-07-09.)

  - name: journaling
    category: Life
    host: mini
    port: 5230
    url: https://memos.tabaska.us
    on_demand: false
    notes: "Self-hosted AI journaling stack (journal-01): Memos front-end (:5230, memos.tabaska.us) + n8n automation (:5678, n8n.tabaska.us) + faster-whisper/speaches (:8010, internal only). Reflections come from rig llama-swap model dolphin-venice-24b. LAN/tailnet only, no cloud. Analyze workflow + tokens wired in journal-02..06."

  - name: meme-review
    category: Life
    host: mini
    port: 8787
    url: https://memes.tabaska.us
    on_demand: false
    notes: "DECOMMISSIONED 2026-07-28 (temporary — container down, tile + Caddy vhost pulled, checks disabled; stack dir + data retained for revival). Self-hosted image-reaction app (meme-review-01) — send stacks of images and react to them one at a time, iMessage-tapback style. Hono + built-in node:sqlite server + Vite/React SPA; SSE realtime; 8-rule achievement engine; optional Immich browse source with local-upload fallback. Imported from a Claude Design prototype (Meme Review.dc.html + HANDOFF.md). LAN/tailnet only."

  # --- Smart Home -----------------------------------------------------------------
  - name: home-assistant
    category: Smart Home
    host: ha
    port: 8123
    url: https://ha.tabaska.us
    on_demand: false
    notes: HA Green — trusted_proxies configured, so ha.tabaska.us serves through Caddy (fix-102 corrected — the old "answers 400 via proxy" note was stale); direct :8123 also works.

  # --- AI & Gaming (rig, 24/7 as of 2026-07-08; rig down = incident) ------------
  - name: open-webui
    category: AI & Gaming
    host: rig
    port: 3000
    url: https://ai.tabaska.us
    on_demand: false
    notes: Chat UI. Rig runs 24/7 — a 502 here is an incident (WoL is recovery tooling).
  - name: litellm
    category: AI & Gaming
    host: rig
    port: 4000
    url: https://llm.tabaska.us
    on_demand: false
    notes: OpenAI-compatible gateway (/ui admin), always-on on the rig. (A mini-fallback stack dir exists in the repo but was NEVER deployed — decision pending, 2026-07-09 audit.)
  - name: lumiverse
    category: AI & Gaming
    host: rig
    port: 3001
    url: https://lumiverse.tabaska.us
    on_demand: false
    notes: Creative/RP chat frontend (prolix-oc/Lumiverse, TS+Bun; pinned by digest = patched v1.0.0+ for the CVE-2026-44449 RCE cluster). Rides LiteLLM via an in-app Custom connection (virtual key scoped to cydonia/dolphin-venice/goetia — the only model gate). App login = BetterAuth (owner seeded from env). Added 2026-07-18. Rig runs 24/7.
  - name: marinara
    category: AI & Gaming
    host: rig
    port: 3002
    url: https://marinara.tabaska.us
    on_demand: false
    notes: Creative/RP chat + game engine (pasta-devs/marinara-engine 1.5.0 image, ALPHA/AGPL). Rides LiteLLM via an in-app connection (same 3-model virtual key). Auth-gated at the mini Caddy (basic_auth, user `marinara`) because its own IP-based auth is void behind docker userland-proxy. Added 2026-07-18. Rig runs 24/7.
  - name: llama-swap
    category: AI & Gaming
    host: rig
    port: 9292
    url: https://llamaswap.tabaska.us
    on_demand: false
    notes: llama.cpp model-swapping server (ai-01, 2026-07-15) — the rig's primary local-model backend (swaps GGUF models on demand); LiteLLM routes local traffic here. Rig runs 24/7.
  - name: ollama
    category: AI & Gaming
    host: rig
    port: 11434
    url: https://ollama.tabaska.us
    on_demand: false
    notes: Native Ollama API — demoted 2026-07-15 (ai-01) to a compat shim for Home Assistant + Obsidian; primary local-model serving moved to llama-swap (:9292). Rig runs 24/7.
  - name: mcpo
    category: AI & Gaming
    host: rig
    port: 8000
    url: https://mcpo.tabaska.us
    on_demand: false
    notes: MCP→OpenAPI tool bridge; docs at /docs (root 404s by design). Rig runs 24/7.
  - name: searxng
    category: AI & Gaming
    host: mini
    port: 8888
    url: https://searxng.tabaska.us
    on_demand: false
    notes: Private metasearch (lai-01, local-ai buildout) — Kagi replacement + the web-search backend for Open WebUI (lai-03). JSON API enabled server-side (search.formats includes json; OWUI strips &format=json from query URLs) and limiter OFF — LAN/tailnet-only. Host port 8888 -> container 8080; browsers via Caddy.
  - name: kiwix
    category: AI & Gaming
    host: nas
    port: 8092
    url: https://kiwix.tabaska.us
    on_demand: false
    notes: Offline ZIM knowledge library (lai-12, local-ai buildout) — kiwix-serve 3.8.2 (tag@digest pinned) on the NAS serving the curated ~150GB set (devdocs picks, iFixit, sysadmin StackExchange, wiktionary en, game wikis, Wikipedia en maxi) from /volume1/zim. Host :8092 -> container :8080 (NAS :8080 is Scrutiny). library.xml rebuilt by kiwix-manage via DSM task 18 nightly 05:15 ONLY when the ZIM set changed (kiwix-serve cannot hot-add ZIMs); downloads land atomically via the sequential queue script. Feeds openzim-mcp (lai-13). Consumer check kiwix-search-consumer.
  - name: maps
    category: AI & Gaming
    host: mini
    port: 8899
    url: https://maps.tabaska.us
    on_demand: false
    notes: Offline maps (lai-17, local-ai buildout) — a static MapLibre viewer + Protomaps "light" basemap over a CONUS PMTiles extract, plus a Photon US geocoder, all behind Caddy at maps.tabaska.us. The pmtiles container = protomaps/go-pmtiles "serve" (host :8899 -> :8080) over data/us.pmtiles (~18GB, pmtiles-extracted from the remote Protomaps daily planet build; the fleet Caddy build has no pmtiles module, so a dedicated serve container is the clean path). Caddy routes /pmtiles/* -> pmtiles, /geocode/* -> photon (:2322, REGION=usa index ~10GB->~17GB, imports on first boot; API answers only after import), else the static viewer from the caddy site dir. Fonts/sprites/maplibre are vendored locally (fetch-maps-assets.sh) so the viewer is fully offline. The cyanheads OSM MCP (opencode) uses PUBLIC OSM Nominatim/Overpass by design; Photon is the offline geocoder consumed by the viewer. Consumer checks maps-pmtiles-serve + maps-photon-geocode.
  - name: open-terminal
    category: AI & Gaming
    host: mini
    port: 8020
    url: null
    ui: false
    on_demand: false
    notes: Sandboxed code-execution backend for the rig's Open WebUI (lai-09, local-ai buildout) — ghcr.io/open-webui/open-terminal 0.11.34 (tag@digest pinned, full toolkit). Replaces browser Pyodide with a real persistent Linux sandbox; OWUI 0.10.2 wires it as a terminal-server connection (PersistentConfig terminal_server.connections, Bearer key vault open_terminal.api_key) and proxies run_command/files tool calls via /api/v1/terminals/open-terminal/*. DELIBERATELY LAN-only, no Caddy vhost, no Homepage tile (it executes arbitrary code); own compose network, no docker socket, mem/cpu/pids capped. Consumer check owui-code-exec.
  - name: kokoro
    category: AI & Gaming
    host: rig
    port: 8880
    url: null
    ui: false
    on_demand: false
    notes: Local TTS for Open WebUI voice (lai-10, local-ai buildout) — remsky/Kokoro-FastAPI v0.7.1, CPU variant tag@digest pinned, in the rig local-ai-tooling compose. CPU on purpose (rig-gpu-vram-contention — no new daytime VRAM consumers); model weights + UniDic baked into the image, no volume. OpenAI-compatible /v1/audio/speech, model "kokoro", 68 voices (default af_heart); OWUI Audio TTS points at http://kokoro:8880/v1 (PersistentConfig, wired via the admin API; compose env = rebuild seeds). KEYLESS — LAN/tailnet posture like llama-swap/comfyui, no Caddy vhost (a /web beta player exists on :8880). STT counterpart = the mini journaling faster-whisper (:8010). Consumer check voice-roundtrip.
  - name: comfyui
    category: AI & Gaming
    host: rig
    port: 8188
    url: https://comfyui.tabaska.us
    on_demand: false
    notes: Image-gen backend (ai-01, added 2026-07-18) serving 3 image models — Z-Image Turbo / NoobAI-XL / Flux.2 Klein — for the RP frontends (OWUI/Lumiverse/Marinara). Frontends do NOT hit :8188 directly; they go through the gpu-arbiter (:8189) so image gen and the local LLMs take turns on the single 3090 Ti. LAN posture (like ollama). Rig runs 24/7.
  - name: local-deep-research
    category: AI & Gaming
    host: rig
    port: 5000
    url: https://ldr.tabaska.us
    on_demand: false
    notes: Iterative deep-research assistant (lai-08, local-ai buildout) — LearningCircuit LDR 1.10.1 (digest-pinned). Multi-step search + synthesis with citations, fully local — LLM = LiteLLM openai_endpoint alias q38 (qwen3.8-27b via llama-swap, scoped virtual key — repointed off coder-strong at lai-30 2026-09-01, key negative-tested), search = the mini SearXNG (lai-01). Per-user SQLCipher-encrypted DBs (no password reset possible); registration open, app login is the auth gate (LAN/tailnet posture like lumiverse). Env-locked config — model/search changes go through the compose, not the UI. Daily consumer check ldr-research-e2e (best-effort under GPU contention).
  - name: unsloth-studio
    category: AI & Gaming
    host: rig
    port: 8210
    url: https://unsloth.tabaska.us
    on_demand: false
    notes: LLM fine-tuning / experimentation web UI (lai-28) on the rig. Provider type must be the HIDDEN llama_cpp (openai type filters lanes empty); auth = bootstrap diceware file, not the env. Rig runs 24/7, LAN/tailnet posture. Catalog row added fix-97 (deploy-completion — lai-28 shipped without it).
  - name: bioclip-api
    category: AI & Gaming
    host: rig
    port: 8199
    url: null
    ui: false
    on_demand: false
    notes: BioCLIP 2 plant/species classifier (lai-22), CPU-by-design. Internal only — no public vhost; consumed by the OWUI identify_plant tool (Plant Scout). Catalog row added fix-97 (deploy-completion — lai-22 shipped without it).
  - name: gpu-arbiter
    category: AI & Gaming
    host: rig
    port: 8189
    url: null
    ui: false
    on_demand: false
    notes: Take-turns GPU-sharing proxy (ai-01) fronting ComfyUI on :8189 — internal only, no web UI/vhost. On an incoming gen it force-unloads llama-swap, proxies to ComfyUI (:8188), then /free's ComfyUI after, so image gen and the 73k-ctx 24B LLMs share one 3090 Ti without OOM. The unload hook must match /api/prompt (the path web-UI/API clients POST), not just /prompt. Rig runs 24/7.
  - name: comfyui-mcp
    category: AI & Gaming
    host: rig
    port: 9000
    url: null
    ui: false
    on_demand: false
    notes: Streamable-HTTP MCP tool server for ComfyUI image gen (lai-11, local-ai-tooling compose) — joenorton/comfyui-mcp-server v1.1.1 built from a pinned git SHA (docker/comfyui-mcp/Dockerfile). Talks to the gpu-arbiter (:8189), never ComfyUI directly, so MCP-driven gens keep the take-turns GPU protocol. 17 tools incl. curated workflow tools zimage_turbo + noobai_anime (docker/comfyui-mcp/workflows, prompt+seed params only). Consumers = OWUI native MCP (3-tool filter) + opencode (full set). LAN posture like llama-swap — :9000, no vhost, no auth. Check image-browser-mcp.
  - name: playwright-mcp
    category: AI & Gaming
    host: rig
    port: 8931
    url: null
    ui: false
    on_demand: false
    notes: Headless-browser MCP tool server (lai-11, local-ai-tooling compose) — microsoft/playwright-mcp v0.0.79 tag@digest pinned, headless chromium only, --isolated (no persistent profile), Host-allowlist gated (entries are host:port; a bare hostname 403s). Streamable-HTTP at :8931/mcp. 24 tools; consumers = OWUI native MCP (8 browse/interact tools) + opencode (full set). LAN-only, no vhost, no auth — never expose (it browses as this host). Check image-browser-mcp.
  - name: apollo
    category: AI & Gaming
    host: rig
    port: 47990
    url: https://apollo.tabaska.us
    on_demand: false
    notes: Game-stream host UI (Apollo — a maintained Sunshine fork, AUR `apollo`; chosen over stock Sunshine for headless/virtual-display support + per-client permissions on the 24/7 headless rig). Drop-in for Moonlight clients — same ports. Self-signed HTTPS upstream on :47990. Rig runs 24/7.
  - name: amp
    category: AI & Gaming
    host: rig
    port: 8080
    url: https://amp.tabaska.us
    on_demand: false
    notes: CubeCoders AMP game panel (MinecraftCross Java+Bedrock instance). Licence is hostname-pinned — never change the compose hostname casually.
  - name: palworld
    category: AI & Gaming
    host: rig
    port: 8211
    url: null
    ui: false
    on_demand: false
    notes: Palworld dedicated server "Robits Farm" (UDP 8211; public = palworld.tabaska.us:1105 via playit; REST admin :8212).
  - name: playit
    category: AI & Gaming
    host: rig
    port: null
    url: null
    ui: false
    on_demand: false
    notes: playit.gg tunnel agent — public game paths on dedicated IP 69.9.181.17 (Java SRV:1105, Bedrock :1111, Palworld :1105). Restart the container after tunnel changes (claim-wedge gotcha).
  - name: bedrock-connect
    category: AI & Gaming
    host: mini
    port: 19132
    url: null
    ui: false
    on_demand: false
    notes: BedrockConnect gateway (UDP 19132) — consoles reach LAN/playit Minecraft via AdGuard featured-server rewrites.
  - name: terraria
    category: AI & Gaming
    host: mini
    port: 7777
    url: null
    ui: false
    on_demand: false
    notes: "Terraria (TShock) co-op — the mini's single always-on light game server (game-01). Join at <mini-ip>:7777 (TCP) on LAN/tailnet; no internet exposure (game-04 owns Tailscale friend reach). REST 7878 is localhost-only for the verification probe."
  - name: romm
    category: AI & Gaming
    host: mini
    port: 8998
    url: https://romm.tabaska.us
    on_demand: false
    notes: RomM retro-game library (container port 8080; mariadb sidecar).

  # --- Infrastructure & Ops --------------------------------------------------------
  - name: syncthing
    category: Infrastructure & Ops
    host: nas
    port: 8384
    url: https://syncthing.tabaska.us
    on_demand: false
    notes: Local-first file-sync hub (foss-03) — NAS hub + mini/rig mesh nodes. GUI admin-auth'd, LAN/Tailscale only; relays + global discovery are DISABLED fleet-wide (no cloud path). Mini node mirror = configs/docker-stack/stacks/syncthing-node.
  - name: bug-triage-evidence
    category: Infrastructure & Ops
    host: mini
    port: 8410
    url: null
    on_demand: false
    ui: false
    notes: Bug-intake evidence server (bug-01) — INTERNAL only, reached by n8n by container name on the edge network. No UI, no Caddy vhost.
  - name: homepage
    category: Infrastructure & Ops
    host: mini
    port: 3010
    url: https://home.tabaska.us
    on_demand: false
    notes: This dashboard. Container port 3000.
  - name: caddy
    category: Infrastructure & Ops
    host: mini
    port: 443
    url: null
    ui: false
    on_demand: false
    notes: The reverse proxy itself — no UI vhost.
  - name: beszel
    category: Infrastructure & Ops
    host: mini
    port: 8090
    url: https://status.tabaska.us
    on_demand: false
    notes: Host/container metrics hub (beszel-agent on mini feeds it; agent has no UI).
  - name: scrutiny
    category: Infrastructure & Ops
    host: nas
    port: 8080
    url: https://scrutiny.tabaska.us
    on_demand: false
    notes: Disk SMART-health hub (glue-10; omnibus = web UI + InfluxDB + NAS collector). mini & rig run scrutiny-collector agents that POST here. No auth (LAN/Tailscale only).
  - name: scrutiny-collector
    category: Infrastructure & Ops
    host: mini
    ui: false
    on_demand: false
    notes: SMART collector agent (glue-10; runs on mini AND rig) — reads local disk SMART and POSTs to the scrutiny hub on the NAS. No web UI.
  - name: uptime-kuma
    category: Infrastructure & Ops
    host: mini
    port: 3001
    url: https://uptime.tabaska.us
    on_demand: false
    notes: Uptime checks. No published status page yet, so Homepage uses siteMonitor not the widget.
  - name: healthchecks
    category: Infrastructure & Ops
    host: mini
    port: 8001
    url: https://health.tabaska.us
    on_demand: false
    notes: Cron/backup dead-man's-switch. Container port 8000. (2026-07-07 unhealthy flap resolved; healthy as of 2026-07-09.)
  - name: adguard-home
    category: Infrastructure & Ops
    host: mini
    port: 53
    url: https://dns.tabaska.us
    on_demand: false
    notes: DNS filtering; admin UI proxied (container :80). Unbound (:5335) is its upstream resolver — no UI.
  - name: adguardhome-nas
    category: Infrastructure & Ops
    host: nas
    port: 53
    url: null
    ui: false
    on_demand: false
    notes: Secondary DNS resolver on the NAS (fix-102 catalog completeness) — the failover for the mini primary. Upstreams are DoH with failover (Quad9 + Cloudflare, fix-89). LAN-only, no public vhost.
  - name: unbound
    category: Infrastructure & Ops
    host: mini
    port: 5335
    url: null
    ui: false
    on_demand: false
    notes: Recursive DNS resolver behind AdGuard — no web UI.
  - name: dockge
    category: Infrastructure & Ops
    host: mini
    port: 5001
    url: https://dockge.tabaska.us
    on_demand: false
    notes: Compose stack manager.
  - name: forgejo
    category: Infrastructure & Ops
    host: mini
    port: 3030
    url: https://git.tabaska.us
    on_demand: false
    notes: Git forge / control repo. SSH on host :2222. Container port 3000.
  - name: ntfy
    category: Infrastructure & Ops
    host: mini
    port: 8080
    url: https://ntfy.tabaska.us
    on_demand: false
    notes: Push notifications. Container port 80.
  - name: diun
    category: Infrastructure & Ops
    host: mini
    port: null
    url: null
    ui: false
    on_demand: false
    notes: Image-update notifier → ntfy. No web UI.
  - name: diun-nas
    category: Infrastructure & Ops
    host: nas
    port: null
    url: null
    ui: false
    on_demand: false
    notes: Image-update notifier on the NAS (fix-102 catalog completeness) — the mini diun watches mini images, this one watches NAS images. → ntfy. No web UI.

  # --- Wiki & Docs -------------------------------------------------------------
  - name: wiki
    category: Wiki & Docs
    host: mini
    port: null
    url: https://wiki.tabaska.us
    on_demand: false
    notes: Static site served by Caddy from /opt/stacks/wiki/site — live; rebuild/publish via scripts/docs/build-wiki.sh.
  - name: principal-ai
    category: Wiki & Docs
    host: mini
    port: null
    url: https://principal-ai.tabaska.us
    on_demand: false
    notes: Static file_server site (Caddy, /opt/stacks/caddy/site/principal-ai-engineer-track) — the principal-AI-engineer track content, linked from the Homepage AI group. Catalog row added fix-97 for catalog-vhost-parity (the vhost shipped without one).

  # --- Private -----------------------------------------------------------------
  - name: stash
    category: Private
    host: nas
    port: 9999
    url: https://stash.tabaska.us
    on_demand: false
    notes: Adult media organizer. LAN/Tailscale only. No API key in vault — no widget.
  - name: whisparr
    category: Private
    host: nas
    port: 6969
    url: https://whisparr.tabaska.us
    on_demand: false
    notes: Adult-scene automation (Whisparr v2, seed-13). LAN/Tailscale only. Feeds a dedicated Stash sub-library; Prowlarr syncs adult-capable indexers only.

# Dead / not running (vhosts commented out in the Caddyfile):
#   tdarr   — REMOVED FROM PLAN 2026-07-08: re-encoding conflicts with TRaSH quality
#             automation and storage isn't scarce. No compose retained (git
#             history has it); stack dir removed from mini 2026-07-19 (fix-43).
#   frigate — NOT DEPLOYED (stack dir staged, no container has ever run)
#   maintainerr — REMOVED FROM PLAN 2026-07-08 (vhost retired 2026-07-09;
#             stack dir removed from mini 2026-07-19, fix-43)
#   dependency-track — RETIRED 2026-07-11 (SBOM feature abandoned; the 3 NAS
#             containers, Kuma monitor, caddy vhost, homepage tile, coverage
#             manifest entries + stack dirs all removed)
