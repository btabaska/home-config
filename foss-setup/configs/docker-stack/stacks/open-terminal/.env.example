# open-terminal .env template (lai-09) — copy to .env (gitignored) and fill in.
# Bearer key required on every open-terminal API request; the rig OWUI
# terminal-server connection holds the same key server-side.
# -> vault open_terminal.api_key
OPEN_TERMINAL_API_KEY=
