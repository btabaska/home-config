# SearXNG secrets — copy to .env (gitignored, never committed) and fill from
# the vault (foss-setup/.handoff-secrets.yaml).
#
# server.secret_key — signs cookies/session tokens. Vault: searxng.secret_key
# Generate a fresh one: python3 -c "import secrets; print(secrets.token_hex(32))"
SEARXNG_SECRET=
