# read-18: ensure NAS CIFS mounts (e.g. /mnt/nas-manga for Suwayomi) are up before
# Docker starts its containers. Ordering only (remote-fs mounts are nofail), so a
# missing NAS never blocks the Docker daemon.
[Unit]
After=remote-fs.target
