[Unit]
Description=Weekly alert-delivery drill (fix-63/SM39)

[Timer]
# Mondays 09:00 EST — a waking hour so the operator can confirm the push landed.
OnCalendar=Mon *-*-* 09:00:00
Persistent=true
RandomizedDelaySec=5min

[Install]
WantedBy=timers.target
