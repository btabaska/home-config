# /etc/default/tv-torrent-cleanup — dead-man ping for tv-torrent-cleanup.service.
# Real URL lives in the vault at healthchecks.tv_cleanup_mini_ping_url.
# Install: root:btabaska 640 (the service runs as btabaska).
HEALTHCHECKS_PING_URL=https://health.tabaska.us/ping/<uuid>
